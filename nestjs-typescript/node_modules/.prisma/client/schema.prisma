generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = "mongodb+srv://phuoctt:2KveYlOxvmlsd4ll@phuoctt.n9eyoar.mongodb.net/youtub?retryWrites=true&w=majority&appName=phuoctt"
}

model playlist {
  id      String @id @default(auto()) @map("_id") @db.ObjectId
  author  String
  avtUser String
  date    String
  id_     Int    @map("id")
  name    String
  src     String
  watched String
}

model users {
  id           String @id @default(auto()) @map("_id") @db.ObjectId
  Email        String
  PasswordHash String
  Role         String
  UserID       Int
  Username     String
}
